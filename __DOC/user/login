/user/login
: 로그인 API

  req: { email, key }
    - email: string
    - key: string
  
  res: { *result, data: { user_id, email } }
    - user_id: string
    - email: string
    - *result
      "void": check_email 필요
      "wrong": 잘못된 인증번호
    + cookie: { session_id }