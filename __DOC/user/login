/user/login
: 로그인 API

  req: { email, keep, key }
    - email: string
    - keep: bool
    - key: string
  
  res: { *result, *message, data: { email, session_id, [ticket] } }
    - email: string
    - session_id: string
    - ticket: string  // if keep is true
    - *result
      "void": check_email 필요