/user/info
: Cookie를 통한 유저 정보 조회

  req: { cookie: { canifeed_uid, canifeed_sid } }
    - canifeed_sid: client에서 처리 X

  res: { *result, data: { user_id, email, photo_url } }
    - email: string
    - user_id: string